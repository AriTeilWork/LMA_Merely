<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.companyname.merelyapp">

  <!-- Permissions -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

  <application
      android:allowBackup="true"
      android:label="MerelyApp"
      android:icon="@mipmap/appicon"
      android:roundIcon="@mipmap/appicon_round"
      android:theme="@style/Maui.SplashTheme"
      android:supportsRtl="true">

    <!-- MainActivity is provided by MAUI and merged into the manifest at build time. Do not declare it here. -->

  </application>

</manifest>
